package ga;

import core.Individual;

public class Crossover {

    public Individual[] uniformCrossover(Individual parent1, Individual parent2, double pc) {
        // TODO: Zaimplementować krzyżowanie jednorodne (Uniform Crossover).
        // Przejść pętlą po genach: z prawdopodobieństwem 0.5 gen dla potomka 1
        // pochodzi od parent1, a z 0.5 od parent2.
        // Wykonać krzyżowanie tylko, jeśli wylosowana liczba < pc. W przeciwnym razie sklonować rodziców.
        // Zwrócić tablicę z dwójką potomków.
        return new Individual[2];
    }
}